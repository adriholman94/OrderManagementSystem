<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" 
xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
xmlns:sec="http://www.springframework.org/schema/security" 
xsi:schemaLocation="
          http://www.springframework.org/schema/beans
          http://www.springframework.org/schema/beans/spring-beans.xsd
          http://www.springframework.org/schema/security
          http://www.springframework.org/schema/security/spring-security.xsd">

    <sec:http auto-config="true" use-expressions="true" pattern="/**" create-session="stateless">
        <!-- url sin proteccion para el acceso del cliente-web -->
        <sec:intercept-url pattern="/products/page/*" access="permitAll()" />
        <sec:intercept-url pattern="/users" access="permitAll()" />
        <sec:intercept-url pattern="/roles/*" access="permitAll()" />
        <sec:intercept-url pattern="/**" access="isAuthenticated()" />
        <sec:http-basic/>
    </sec:http>

    <sec:authentication-manager alias="authenticationManager">
        <sec:authentication-provider>
            <sec:password-encoder ref="encoder" />
            <sec:jdbc-user-service data-source-ref="dataSource" users-by-username-query="select user_name, user_password as password from users where user_name = ?" authorities-by-username-query="select u.user_name, r.role_name as authority 
            from users u, users_roles ur, roles r
                where u.user_id = ur.user_id
                and r.role_id = ur.role_id 
                and u.user_name  = ?" />
        </sec:authentication-provider>
    </sec:authentication-manager>

    <!-- bean para el encoder de password-->
    <bean id="encoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />

    <!--anotacion @Secured de Spring -->
    <sec:global-method-security secured-annotations="enabled"/>
</beans> 